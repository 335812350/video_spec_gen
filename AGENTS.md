# 仓库 Agent 说明

## HyperFrames 预览生命周期

- 每个任务 / 项目只保留一个 HyperFrames 预览服务器。启动前先运行
  `npx hyperframes preview --status`；如果已有服务器正在服务当前项目，就复用它。
- 同一个任务使用一个稳定端口（默认 `3002`）。不要因为其他命令需要 URL，
  就为同一任务再次启动 `preview`、`play` 或 `present` 服务器。只有在同时处理
  完全不同的任务 / 项目时，才使用其他端口。
- Agent 启动预览时优先使用 `--no-open`。不要在脱离终端或后台 shell 中启动前台预览。
- 审阅完成后，使用 `npx hyperframes preview --stop` 停止受管理的预览服务器。
  渲染不需要预览服务器；MP4 渲染期间或完成后都不要让预览服务器继续运行。
- 如果预览命令报告端口已被占用，先检查现有服务器。停止失效服务器，或有意为另一个
  任务分配独立端口；不要为同一个任务不断递增端口重试。

## 素材授权与隐私

- 用户放入 `assets/<film-slug>/` 的素材，默认视为用户已获得合法授权，可用于当前影片的
  剪辑、合成、变形和渲染。
- 对这些用户提供的素材，不要反复提出版权、隐私或侵权警告，也不要因此阻止正常的剪辑方案。
- 当前工作以本地制作和效果验证为目的，不以公开发布为前提。
- 保留素材的原始路径和来源信息；不要擅自替换、下载或引入外部素材。
- 只有在用户明确要求公开发布，或素材并非用户提供、来源确实不明时，才重新讨论版权、隐私
  或授权问题。

## 多影片工作区布局

- 本仓库是多部影片的集合工作区。每部影片使用稳定的 kebab-case
  `<film-slug>`；读取或写入影片产物前，必须先确定目标影片。
- `assets/<film-slug>/` 是输入边界：存放用户提供的源视频、音频、字幕、图形、参考资料
  及其他源素材。不要把生成的 spec、项目、预览或渲染结果写入 `assets/`。
- `projects/<film-slug>/` 存放可编辑的工作产物，例如 `edit-plan.md`、`video-spec.md`、
  分镜文件和 HyperFrames 合成项目。若渲染器需要暂存素材副本，可以放在此项目目录下，
  但权威源文件仍然位于 `assets/<film-slug>/`。
- `outputs/<film-slug>/` 存放可观看的渲染版本，文件名为 `render-vNNN.mp4`。不设
  `final/` 或 `publish/` 子目录；由用户选择要保留或使用的版本。
- 现有的 `videos/<film-slug>/` 文件夹属于旧版 HyperFrames 工作目录。不要把它们作为
  新影片 spec 或渲染结果的默认位置；只有用户明确指定路径时才检查这些目录。
- 永远不要覆盖已有的渲染版本。每次新渲染都递增数字版本号。
- 仓库根目录的 `video-spec.md` 仅作为旧版材料保留。不要在仓库根目录创建新影片 spec，
  也不要在没有明确指示的情况下移动或删除旧文件。
