# 项目进展

> 仓库：`video_spec_gen`
> 
> 最近更新：2026-09-07

## 当前阶段

工作流和项目目录正在从早期原型整理为可持续迭代的仓库结构。高能混剪已完成第一版，影视解说工作流 v1 也已完成首个项目的正式渲染；当前重点是审片、迭代、二次复检和正式交付闭环。

## 已完成

> 时间依据：以下时间按 Git 提交、项目状态记录、QA 记录和实际产物时间整理；没有独立完成记录的基础事项按首次可核对日期归档。

### 2026-09-02：基础架构与高能混剪首版

- [x] 建立 `assets/<film-slug>/`、`projects/<project-slug>/`、`outputs/<project-slug>/` 的目录约定。
- [x] 将项目规格和工程文件迁移到 `projects/<project-slug>/`。
- [x] 明确 `video-spec.md`、`edit-plan.md`、HyperFrames 工程和渲染输出的职责边界。
- [x] 完善 `video-spec-director-dev` 的项目布局、规格模板、素材研究、分镜和迭代流程。
- [x] 增加 `video-production-dispatcher` 工作流调度 skill 和注册表。
- [x] 增加 `high-energy-clip` skill 与 `tools/high_energy_analyzer.py`。
- [x] 创建年会高能混剪项目：
  - `projects/annual-meeting-high-energy-90s/`
  - `projects/annual-meeting-high-energy-rap-full/`
- [x] 完成年会高能片段剪辑第一版。
- [x] 统一使用阿里云 TTS CLI，并保留兼容入口和离线测试。
- [x] 补充自主视频生产平台总设计、当前架构图、共享能力目录和多智能体生产说明。
- [x] 清理根目录遗留的 `video-spec.md`、`videos/`、`debug.log`、电视推荐审查文件和空的 `outputs/smoke-console/`。

### 2026-09-02 至 2026-09-03：影视解说工作流 v1 与首个项目

- [x] 完成 `film-commentary` 影视解说工作流 v1：新增 `movie-master-director`、索引、执笔、配音、编导、渲染 QA 和二次审片阶段 Skill，并接入调度层注册表。
- [x] 增加影视解说工作流文档和自动化测试，覆盖阶段状态、正式分镜门槛、阿里云配音和渲染 QA。
- [x] 完成首个影视解说项目 `projects/nianhui-bunengting-commentary/`：生成影片索引、故事地图、旁白、`edit-plan.md`、57 Scene 的正式 `video-spec.md` 和 HyperFrames 工程。
- [x] 完成影视解说第一版成片及修订版：`outputs/nianhui-bunengting-commentary/render-v001.mp4` 和 `render-v002.mp4`，670 秒；`2026-09-03` 的渲染前规格预检与渲染后技术检查均通过。

### 2026-09-06：《浮生之白蛇前缘》多视角项目首轮规格

- [x] 完成新影片资料补全：assets/fusheng-zhi-baishe-qianyuan/references/ 下的 film-metadata.json、film-profile.md、story-context.md 和 media-manifest.json。
- [x] 下载并登记两条公开预告样片和缩略图；保留 B 站 412 风控失败与 WeTV 抽取失败记录，未伪造正片时间码。
- [x] 创建 B 站 4 分钟 16:9 多视角项目：projects/fusheng-zhi-baishe-qianyuan-bilibili-multiview/。
- [x] 完成共同主叙事、文化考古替换模块、当代改编观察替换模块、AI 旁白脚本、edit-plan.md 和 video-spec.md。
- [ ] 待人工审片补齐公开预告的精确 source 时间码，查询并试听阿里云 TTS voice ID，补齐文化图像与 BGM 的下载和授权记录后再进入 HyperFrames 渲染。

### 2026-09-07：HyperFrames 最新运行时

- [x] 将 `projects/nianhui-bunengting-commentary/hyperframes/package.json` 中的 HyperFrames 固定版本从 `0.8.26` 升级到最新 `0.8.30`。
- [x] 在 `E:\pyproject\hyperframes-runtime\0.8.30` 安装独立版本化运行时，npm 临时缓存使用 `E:\Temp\npm-cache`，不修改全局环境变量。
- [x] 使用 `0.8.30` 对影视解说 HyperFrames 工程执行 `check`：0 errors、0 warnings、0 runtime errors、0 layout issues、0 motion errors，WCAG 对比度 15/15 通过。
- [x] 将现有 4 个 HyperFrames 项目统一固定到 `0.8.30`，并分别完成升级后的兼容性 `check`。
- [x] 明确 HyperFrames Skill 作用域：本项目以 `.agents/skills/` 和 `skills-lock.json` 为权威；C 盘 `C:\Users\chenyinghong\.codex\skills` 仅作为 Codex 全局 Skill 目录，不作为项目 Skill 依赖。

- [x] 将 26 个项目级 HyperFrames Skill 更新到上游 heygen-com/hyperframes 主分支提交 0d5d3f3eb3aecd9fd64954d2767d3d64e97e58fc，并修复 skills-lock.json 中历史 skills/... 路径为 .agents/skills/...，重建内容哈希。
- [x] 按更新决策采用最新上游 captions-overlay 与 embedded-captions 规则；影视解说字幕与画面保护约束仅由 movie-master-director、movie-direct 和 movie-render-qa 等专属 Skill 维护，不再改写通用 HyperFrames Skill。
- [x] 验证 test_film_commentary_workflow.py（7 项）和 test_workflow_routing.py（6 项）全部通过。

### 2026-09-07：旁白音色锁定

- [x] 2026-09-07：根据 docs/qwen-audio-3.0-tts-plus基础音色.xlsx 锁定《浮生之白蛇前缘》旁白为 qwen-audio-3.0-tts-plus + qwen-audio-3.0-tts-plus-longhuiyanxing（龙辉焰杏）；试听音频已生成并通过 ffprobe 参数检查。

- [x] 2026-09-07：修复《浮生之白蛇前缘》项目 HyperFrames CLI 版本解析，初始化 projects/fusheng-zhi-baishe-qianyuan-bilibili-multiview/hyperframes/，固定 hyperframes@0.8.26；离线 check --json 通过（lint/runtime/layout 0 错误）。

- [x] 2026-09-07：完成《浮生之白蛇前缘》B站多视角 culture 草稿渲染：outputs/fusheng-zhi-baishe-qianyuan-bilibili-multiview/render-v001.mp4；ffprobe 验证 240.0s、1920x1080、30fps、H.264/AAC。

- [x] 2026-09-07：完成《浮生之白蛇前缘》B站多视角草稿修订版 render-v002.mp4：移除从左到右的渐变 wash，改为统一纯色压层；新增旁白字幕轨；ffprobe 验证 240.0s、1920x1080、30fps、H.264/AAC。


### 2026-09-08：业务视频 Skill Pack

- [x] 新增 `coocaa_video/skills/` 对外分发结构，包含 `video-workspace` 与 `video-spec-director` 两个业务入口 Skill。
- [x] `video-workspace` 提供本地视频工作区模板：`setup.ps1`、`setup.sh`、`doctor.ps1`、`doctor.sh`、`.env.example`、`video-spec.md`、`assets/README.md`、`projects/`、`outputs/`。
- [x] 补充 `coocaa_video/README.md` 与两个 Skill 的 `INSTALL.md`，明确全局安装和项目级安装方式；Windows `doctor.ps1` 已在当前仓库环境验证通过。
- [x] `video-spec-director` 分发版已全量同步内部 Skill 资源，并移除对 `video-production-dispatcher` / 调度层路由的依赖；`workflow_id` / `workflow_version` 调整为可选记录。
- [x] README 已按业务用户首次使用流程重写，覆盖安装、初始化工作区、放入素材、生成/迭代 video-spec 和故障排查。
- [x] `video-workspace` 目录契约已对齐 `video-spec-director`：`assets/<film-slug>/references/`、`projects/<project-slug>/`、`outputs/<project-slug>/`，并同步更新模板 README、doctor 提示和主 README。
- [x] 明确 `.codex-tmp/<project-slug>/` 临时目录契约：只存当前任务临时文件，任务结束后清理；已同步 workspace Skill、README、USAGE 和 doctor 提示。
- [x] ideo-spec-director 中英文 README 已统一为 Codex 使用说明，并移除不适用的 Spec Mono 主题介绍、外链和分发版 spec-mono/ 目录。
- [x] `coocaa_video/README.md` 已梳理两个 Skill 的职责边界：`video-workspace` 负责初始化工作区，`video-spec-director` 负责生成/迭代 `video-spec.md`，并说明与 HyperFrames 渲染的上下游关系。
- [x] 安装说明中的占位仓库地址已替换为 `https://github.com/335812350/video_spec_gen`，覆盖 Skill Pack README 与两个 Skill 的 INSTALL.md。
- [x] 内部 `.agents/skills/video-spec-director` 已更名为 `video-spec-director-dev`，相关 Skill、调度注册表、文档、测试和 console 引用已同步；对外 `coocaa_video/skills/video-spec-director` 名称保持不变，避免 skills add 安装歧义。
- [x] 安装建议已调整为“当前项目安装优先，全局安装备选”，并同步 Skill Pack README 与两个 Skill 的 INSTALL.md。

### 2026-09-08：HyperFrames 使用教程

- [x] 新增 docs/hyperframes-usage-guide.md：面向业务用户和团队成员的 HyperFrames 中文使用教程，覆盖环境准备（Node 22+、FFmpeg、doctor）、init/check/preview/render 完整循环、Codex 日常路径、常用命令速查和常见坑。

## 进行中

- [x] 2026-09-04：完成影视解说工程无黑色遮罩审计：移除全局 footage wash、所有章节卡整屏黑底和字幕重阴影；保留 2.40:1 原片在 16:9 画布中的必要留黑，并将审计结果写入项目 RENDER_QA.md。

- [ ] 根据第一版审片反馈完善高能混剪的素材选择、节奏和剪辑规格。
- [ ] 生成并确认高能混剪第二版，完成最终交付前检查。
- [ ] 完成影视解说 `render-v002.mp4` 的可选独立时间码二次审片，并将问题回流到项目记录。
- [ ] 将更多新增工作流与统一 `video-spec` 契约、质量检查和项目运行记录进一步衔接。
- [ ] 根据实际使用情况补充更多工作流注册项和对应测试。

## 待完成

- [ ] 决定 `console/` 是否继续作为后续产品方向；当前它不是主架构，但源码和产品文档仍保留。
- [ ] 为仓库增加更完整的项目级状态、运行记录和验收结果关联。
- [ ] 完成批量子任务编排、子任务状态传播、受控合并和恢复机制。
- [ ] 建设参考视频拆解能力：支持从参考视频提取结构、镜头、节奏、字幕、声音和视觉规则，产出可复用的规则包，并接入视频类型工作流生成 `video-spec.md`。
- [ ] 完善 Skill 能力注册链路：建立统一 Skill manifest、输入输出与依赖声明、注册与发现、自动能力校验、版本兼容、弃用和维护流程。
- [ ] 完善 BGM 生产链路：建立选曲与情绪匹配、节奏同步、旁白/音效避让、音量自动化和最终混音验收规则，让 BGM 成为视频叙事与节奏的重要组成部分。
- [ ] 处理现有 TTS clone 测试中伪造 WAV 与本机 `ffprobe` 质量校验不兼容的问题。
- [ ] 评估并清理剩余临时文件、缓存、预览截图和重复 skill。

## 已知问题与注意事项

- 完整 Python 测试目前有 1 项失败：TTS clone 测试使用最小 WAV 夹具时，本机 `ffprobe` 将其判定为无效音频；相关实现文件未因此跳过质量校验。
- `assets/`、`outputs/`、项目生成音频、预览截图和依赖目录默认是本地或可再生内容，不应直接提交。
- `.env.local` 只用于本地密钥配置，不得提交或写入日志、manifest、spec。
- `console/` 当前仅作为历史/后续实现参考，除非明确要求，不作为其他功能的默认修改范围。
- HyperFrames `0.8.30` 检查已通过，但 `annual-meeting` 仍有 2 个对比度 warning，`annual-meeting-high-energy-90s` 仍有 19 个 `studio_missing_editable_id` warning；这些不阻断当前 check，但应在后续质量迭代中处理。
- E 盘 HyperFrames `0.8.30` 本地 CLI 的 Skill 检查/安装目标与项目本地 `.agents/skills/` 是两个作用域；重新安装 npm 包可能覆盖本地 CLI 补丁，项目 Skill 仍以仓库文件和 `skills-lock.json` 为准。

## 最近提交

- 分支：`codex/workflow-routing-and-high-energy`
- 提交：`6fee606 feat: add workflow routing and high-energy video pipeline`
